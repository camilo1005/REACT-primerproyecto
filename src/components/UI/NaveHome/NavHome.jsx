import React from 'react'

export const NavHome = ({textNav, ref}) => {
  return (

    <nav className='barranavegacion'>
        <a href='#'>Home</a>
        <a href='#'>AboutUs</a>
        <a href="#">ContactUs</a>
    </nav>
  )
}
